.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:sounds, :index)
      %li.active=link_to pat(:new), url(:sounds, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:sound)
    .inner
      -form_for :sound, url(:sounds, :create), :class => :form do |f|
        =partial "sounds/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")
